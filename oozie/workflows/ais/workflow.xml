<workflow-app name="shell-wf" xmlns="uri:oozie:workflow:0.5">
    <start to="ais-run-flow"/>
	<action name="ais-run-flow">
		<shell xmlns="uri:oozie:shell-action:0.1">
			<job-tracker>${jobTracker}</job-tracker>
			<name-node>${nameNode}</name-node>
			<configuration>
				<property>
					<name>mapred.job.queue.name</name>
					<value>${queueName}</value>
				</property>
			</configuration>
			<exec>${aisShellScript}</exec>
			<argument>-l</argument>
			<argument>${aisFolder}</argument>
			<argument>-a</argument>
			<argument>${pentahoStart}</argument>
			<file>${aisPath}${aisShellScript}#${aisShellScript}</file>
			<capture-output/>
		</shell>
		<ok to="endMain"/>
		<error to="failEmail"/>
	</action>
	<action name="failEmail">
		<email xmlns="uri:oozie:email-action:0.1">
			<to>${alertEmail}</to>
			<subject>oozie job failure</subject>
			<body>The workflow ${wf:id()} name ${wf:name()} path ${wf:appPath()} had issues and was killed.  The error message is: ${wf:errorMessage(wf:lastErrorNode())}</body>
		</email>
		<ok to="fail" />
		<error to="fail"/>
	</action>
	<kill name="fail">
		<message>Shell command failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
	</kill>
	<end name="endMain"/>
</workflow-app>
